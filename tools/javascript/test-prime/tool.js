/**
 * Prime Factorization
 * 
 * Decompose a number into its prime factors.
 * 
 * @category math
 * @input { "number": 84 }
 */

const input = JSON.parse(process.argv[2] || '{}');
console.log(input);
