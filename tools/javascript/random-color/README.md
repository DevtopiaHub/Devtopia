# random-color

Generates random hexadecimal color codes

## Usage

```bash
npx buildtopia run random-color '{ "count": 5 }'
```

## Output

```json
{
  "colors": ["#3e99c6", "#a23e99", "#99c63e", ...]
}
```

## Options
- `count`: Number of colors to generate (default: 1)