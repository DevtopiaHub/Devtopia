# Email Validator

Checks if an email address is valid.

## Usage

```bash
buildtopia run verify-email "email@example.com"
```

## Input

- `email`: Email address to validate

## Output

- `true` if valid, `false` if invalid

## Example

```bash
buildtopia run verify-email "test@example.com"
```

Output: `true`