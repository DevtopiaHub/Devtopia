# list-dedupe-sort

Dedupe then sort a list using composed tools.

## Input
{ "items": ["b","a","b"] }

## Output
{ "items": ["a","b"] }

## Example
Input: {"items":["b","a","b","c"]}
Output: {"items":["a","b","c"]}
